CFH:
  rules:
    - when:
        fusions_called:
          not_empty: true
      status: "intermediate"

CFHR3:
  rules:
    - when:
        fusions_called:
          not_empty: true
      status: "intermediate"

CYP11B1:
  rules:
    - when:
        fusions_called:
          not_empty: true
      status: "pathological"

f8:
  rules:
    - when:
        sv_called:
          not_empty: true
      status: "pathological"

GBA:
  rules:
    - when:
        fusions_called:
          not_empty: true
      status: "intermediate"

ikbkg:
  rules:
    - when:
        deletion_haplotypes:
          not_empty: true
      status: "pathological"

ncf1:
  rules:
    - when:
        gene_cn: 0
      status: "pathological"

smn1:
  rules:
    - when:
        smn1_cn: { "<": 2 }
      status: "pathological"